## What does this PR do?

<!-- One or two sentences. What changed and why. -->

## Checklist

- [ ] `tsc --noEmit` passes locally
- [ ] `npm run build` passes locally
- [ ] No hardcoded secrets or API keys committed
- [ ] New env vars added to `.env.example` and GitHub Secrets
- [ ] UI changes tested in a browser (happy path + edge cases)
- [ ] If touching payment or auth flows — manually verified the full flow
